const numbers = [9, 4, 13, 2, 20, -1, 0];

const insertionSort = function (arr) {
  for (let i = 0; i < arr.length; i++) {
    let j = i + 1;
    while (j > 0) {
      //TODO compare and move back
      j--;
    }
  }
};
